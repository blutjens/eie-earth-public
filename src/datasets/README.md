Here live dataloaders (functions for fetching, not actual datasets).

Intended for use mainly with PyTorch Lightning, a dataset should have a class that implements dunder methods for `getitem` and `len`.
